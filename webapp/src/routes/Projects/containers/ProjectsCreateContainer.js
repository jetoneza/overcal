import { connect } from 'react-redux'

import ProjectsForm from '../components/ProjectsForm'

const mapActionCreators = {}

const mapStateToProps = (state) => ({})

export default connect(mapStateToProps, mapActionCreators)(ProjectsForm)


